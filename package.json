{
  "name": "docslurp-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist",
    "dev": "npm run dev --workspace=packages/cli"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
